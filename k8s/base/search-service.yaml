apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  namespace: shiftly
spec:
  replicas: 2
  selector:
    matchLabels:
      app: search-service
  template:
    metadata:
      labels:
        app: search-service
    spec:
      containers:
        - name: search-service
          image: your-registry/search-service:latest
          ports:
            - containerPort: 3000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: shiftly-secrets
                  key: DATABASE_URL
            - name: KAFKA_BROKERS
              value: "kafka.shiftly.svc.cluster.local:9092"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: shiftly
spec:
  type: ClusterIP
  selector:
    app: search-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
